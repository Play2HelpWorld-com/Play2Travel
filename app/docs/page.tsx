import Docs from "@/components/Docs";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Play2Travel | Play for Free Donation",
  description: "This is built by Moyasi",
  // other metadata
};

export default function DocsPage() {
  return <Docs />;
}
